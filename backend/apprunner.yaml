version: 1.0
runtime: python310 # Or the Python version you are using
build:
  commands:
    build:
      - "echo Build started"
run:
  command: uvicorn src.api:app --host 0.0.0.0 --port 8080
  network:
    port: 8080
healthcheck:
  path: /docs # FastAPI provides a default health check endpoint